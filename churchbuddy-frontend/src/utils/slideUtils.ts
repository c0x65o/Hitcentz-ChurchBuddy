export const createDefaultSlideHTML = (text: string): string => {
  // Single text box - always use h1 for consistent styling
  return `<h1>${text}</h1>`;
};

export const createWorshipSlideHTML = (text: string): string => {
  return `<h1>${text}</h1>`;
};

export const createVerseSlideHTML = (text: string): string => {
  return `<h1>${text}</h1>`;
}; 